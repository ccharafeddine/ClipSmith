node_modules
dist

# Default export output folder — local clips, never committed.
Exports/

# FFmpeg LGPL sidecars — fetched by scripts/fetch-ffmpeg.sh, never committed.
# CI must run the fetch script before building.
src-tauri/binaries/*
!src-tauri/binaries/.gitkeep
